{
  "name": "airbnb-property-issue-management",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && NODE_OPTIONS=--max-old-space-size=4096 npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "uuid": "^9.0.0",
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "resend": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
