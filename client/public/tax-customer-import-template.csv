email,name,phone,whatsapp,locale,address_line1,address_line2,city,state,postal_code,country,preferred_communication_email,notes,relationships
# ─────────────────────────────────────────────────────────────────────────────
# Tax customer import template
# ─────────────────────────────────────────────────────────────────────────────
#
# Fill in one row per customer below the header. Delete the comment lines
# (anything starting with "#") and the sample row at the bottom once you've
# finished — though if you forget, the importer skips comment lines safely.
#
# Save as CSV (Excel: "CSV UTF-8") and upload at:
#   {your-portal}/tax/{community-slug}/employee/customers → Import from CSV
#
# ── Columns ──────────────────────────────────────────────────────────────────
#
# email        REQUIRED.  Customer's sign-in email.  Must be unique within
#              the practice.  Case-insensitive.
# name         Customer's full name.
# phone        Free-text phone.  Any format accepted.
# whatsapp     International format starting with + and country code (e.g.
#              +14155551234).  Spaces, dashes, and parens are tolerated but
#              the saved value is normalized to digits.  Leave blank if
#              you don't know it.
# locale       'en' or 'es'.  Default: 'es'.  Controls the language used in
#              email reminders + portal interface defaults.
# address_line1 / address_line2 / city / state / postal_code
#              Standard mailing-address subfields.
# country      Two-letter country code (US, MX, etc.).  Defaults to US when
#              address_line1 is filled in.
# preferred_communication_email
#              Optional alternate email.  When set, reminders + document
#              notifications go here instead of the sign-in email.
# notes        Internal notes — only the practice (you and your staff) see
#              these, never the customer.
# relationships
#              Comma-separated list of service tags.  Use the ids exactly
#              as shown.  Valid values for this practice:
#
#                business.llc
#                business.s_corp
#                business.partnership_1065
#                business.sales_tax_filing
#                business.business_formation
#                business.payroll
#                business.bookkeeping
#                individual.taxes
#                individual.itin
#                general.notary
#                general.translation
#                audit.irs
#                audit.drs
#
#              Wrap the list in double quotes if it contains commas, e.g.:
#                "business.llc,business.bookkeeping,business.payroll"
#
# ── Import behavior ──────────────────────────────────────────────────────────
#
# Duplicate handling — at upload time you choose:
#   Skip existing   (default)  Existing emails are left untouched.  Safe to
#                              re-upload the same file as many times as you
#                              like; only brand-new emails get created.
#   Update existing            Existing emails get a field-level merge —
#                              blank cells preserve the current value, non-
#                              blank cells overwrite.  Relationships are
#                              additively merged: tags from the CSV are
#                              added to whatever the customer already has;
#                              tags are never removed by an import.
#
# After upload you'll see a summary:  Created · Updated · Skipped · Errors
# plus per-row drill-down for anything that needed attention.
#
# ── Sample row (delete before importing your real customers) ─────────────────
sample.customer@example.com,Sample Customer,(415) 555-1234,+14155551234,en,123 Main St,Suite 4,San Francisco,CA,94103,US,billing@example.com,New client referred by Maria,"business.llc,business.bookkeeping"
